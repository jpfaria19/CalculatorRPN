package infnet.gads.joaolfaria.calculadorarpn;

public class MyStack {

    /*
    public final int CAPACITY = 1000;

    private double[] container;

    private int top = -1;

    public MyStack(){
        container = new double[CAPACITY];
    }

    public void push(double value){ //Método de ENTRADA na pilha.

        if (top<CAPACITY){
            top = top++;
            container[top] = value;
        }
    }

    public double pop(){ //Método de REMOÇÃO na pilha.
        if (top >=0){
            double value = container[top];
            top = top--;
            return value;
        }
        return 0.0;
    }
    */
}
